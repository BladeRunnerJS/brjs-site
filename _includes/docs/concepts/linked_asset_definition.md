_Assets_ that contain references to other _assets_ that should also be bundled are known as _linked-assets_, and in BladeRunnerJS these include the following asset types:

* JavaScript Source
* HTML Templates
* XML Configuration
