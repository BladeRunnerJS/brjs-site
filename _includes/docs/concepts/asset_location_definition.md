_Asset-Locations_ on the other hand represent the exact directories within an _asset-container_ where _assets_ can be found. The following are all _asset-locations_ for example:

* `resources`
* `src`
* `src-test`
